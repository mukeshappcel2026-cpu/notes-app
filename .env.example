# Server configuration
PORT=3000
NODE_ENV=development

# AWS configuration
AWS_REGION=us-east-1
DYNAMODB_TABLE=Notes

# DynamoDB Local endpoint (only for local development)
# Uncomment the line below when using docker-compose
# DYNAMO_ENDPOINT=http://localhost:8000

# CORS
ALLOWED_ORIGIN=*
